{
  "name": "stripe-firebase",
  "version": "0.3.0",
  "description": "This is library for stripe and firebase. Forked and updated to include trial days",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "stripe",
    "firebase"
  ],
  "author": "Dragos Catalin (https://github.com/dragoscv)",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/dragoscv/stripe-firebase.git"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  },
  "peerDependencies": {
    "firebase": "^11.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}